# Production: set the same value in Vercel → Settings → Environment Variables (not only here).
# VITE_API_URL=https://your-service.onrender.com
#
# Local dev: leave unset to use Vite /api proxy, or use:
# VITE_API_URL=http://127.0.0.1:8004
